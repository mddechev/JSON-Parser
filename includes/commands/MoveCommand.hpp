// #pragma once

// #include "Command.hpp"


// class MoveCommand: public Command {
// public:
//     MoveCommand(JSONManager* const manager);

//     bool execute(const Vector<String>& tokenizedCmd) override;

// private:
//     bool validate(const Vector<String>& tokenizedCmd) override;
// };

